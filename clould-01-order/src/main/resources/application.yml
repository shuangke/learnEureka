server:
  port: 8001

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # the address of the Eureka server that the service will register with